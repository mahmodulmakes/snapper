// Empty on purpose. Hard Rule 4 (CLAUDE.md) requires an explicit preload for
// every BrowserWindow, "no exceptions, including temporary debug windows" —
// this spike's overlay window has no IPC surface to expose, so this is a
// deliberate no-op rather than an omission.
